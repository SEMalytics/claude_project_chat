# Option 1: Claude.ai Web Client (Recommended for Projects Mode)
# Get this from your browser's DevTools when logged into claude.ai
# 1. Open claude.ai and log in
# 2. Open DevTools (F12)
# 3. Go to Application tab (or Storage tab in Firefox)
# 4. Click Cookies -> https://claude.ai
# 5. Find "sessionKey" in the list
# 6. Right-click the sessionKey value and Copy (just the value, not the name)
# Paste the value here - the code will add "sessionKey=" automatically
CLAUDE_COOKIE=

# Optional - Start in a specific conversation
CLAUDE_CONVERSATION_ID=

# Option 2: Anthropic API (For API Mode)
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# Required if using API mode - Your Claude Project UUID
# Get this from your project URL: https://claude.ai/project/{UUID}
CLAUDE_PROJECT_ID=your-project-uuid-here

# Optional - Server Configuration
FLASK_ENV=development
FLASK_DEBUG=False
HOST=127.0.0.1
PORT=5000

# Optional - Upload Limits
MAX_FILE_SIZE=10485760

# Optional - Google Custom Search (for web_search tool)
# If not set, DuckDuckGo is used as the default (no API key needed)
# To use Google Custom Search:
# 1. Create a Custom Search Engine: https://programmablesearchengine.google.com/
# 2. Get an API key: https://console.cloud.google.com/apis/credentials
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_CX=
